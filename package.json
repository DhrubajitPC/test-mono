{
  "name": "test-mono",
  "private": true,
  "scripts": {
    "deploy:app1": "cd apps/hello-react && wrangler pages deploy",
    "deploy:app2": "cd apps/static-pages && wrangler pages deploy",
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "turbo": "^2.4.0",
    "typescript": "5.7.3"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
